class DOMParser{
	constructor(tokenizer){
		this.tokenizer = tokenizer;
	}

	/*
		<DOM> = <element> 
		<element> = (<open_tag> | <open_close_tag>) | <content> | (<closed_tag>)
		<open_tag> = '<'
	*/

	parseDOM(raw){

	}
}